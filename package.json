{
  "name": "@studiotec-arch/image-compress",
  "version": "1.0.1",
  "description": "Compressão de imagem no browser antes do upload — biblioteca compartilhada do ecossistema Studiotec.",
  "license": "UNLICENSED",
  "//private": "O repositório é público para os apps instalarem sem credencial, mas o pacote não vai a registro nenhum — `private` evita um `npm publish` acidental.",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "sideEffects": false,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.json && node --test test/*.test.js",
    "prepare": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Studiotec-Arch/image-compress.git"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2"
  }
}
